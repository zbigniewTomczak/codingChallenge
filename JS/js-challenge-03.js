var assert = require('assert');
/* Truncate a string!
Truncate a string (first argument) if it is longer than the
given maximum string length (second argument).
Return the truncated string with a ... ending.

Note that inserting the three dots to the end WILL
add to the overall string length.

However, if the given maximum string length num is
less than or equal to 3,
then the addition of the three dots does not
add to the string length
in determining the truncated string.

truncate(str, num)

*/

function truncate(str, num) {

}

it('', function() {
    //given
    //when

    //then
    assert.equal(true, true);
});
